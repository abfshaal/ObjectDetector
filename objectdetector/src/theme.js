import { blue, grey } from "@mui/material/colors";

const theme = {
  palette:{
    mode:'dark',
    primary: {
      main: grey[800]
    },
    secondary:{
      main: blue[400]
    }
  }
}

export default theme